diff --git a/emscripten_build_file.bzl b/emscripten_build_file.bzl
index 0d7aff2..07809ff 100644
--- a/emscripten_build_file.bzl
+++ b/emscripten_build_file.bzl
@@ -91,3 +91,10 @@ filegroup(
     ),
 )
 """
+
+filegroup(
+    name = "dwp_files",
+    srcs = [
+        "bin/llvm-dwp{bin_extension}",
+    ],
+)
diff --git a/emscripten_toolchain/emar.bat b/emscripten_toolchain/emar.bat
index c806808..bb147b3 100644
--- a/emscripten_toolchain/emar.bat
+++ b/emscripten_toolchain/emar.bat
@@ -2,4 +2,4 @@
 
 call %~dp0\env.bat
 
-py -3 %EMSCRIPTEN%\emar.py %*
+%EMSDK_PYTHON% %EMSCRIPTEN%\emar.py %*
diff --git a/emscripten_toolchain/emar.sh b/emscripten_toolchain/emar.sh
index b4ead6e..d57222f 100755
--- a/emscripten_toolchain/emar.sh
+++ b/emscripten_toolchain/emar.sh
@@ -2,4 +2,11 @@
 
 source $(dirname $0)/env.sh
 
-exec python3 $EMSCRIPTEN/emar.py "$@"
+if [[ "$EMSDK_PYTHON" != /* ]]; then
+    EMSDK_PYTHON="$ROOT_DIR/$EMSDK_PYTHON"
+fi
+
+PYBINPATH="$(dirname "${EMSDK_PYTHON}")"
+export PATH=$PYBINPATH:$PATH
+
+exec $EMSDK_PYTHON $EMSCRIPTEN/emar.py "$@"
diff --git a/emscripten_toolchain/emcc.bat b/emscripten_toolchain/emcc.bat
index 4088bc4..520135f 100644
--- a/emscripten_toolchain/emcc.bat
+++ b/emscripten_toolchain/emcc.bat
@@ -2,4 +2,4 @@
 
 call %~dp0\env.bat
 
-py -3 %EMSCRIPTEN%\emcc.py %*
+%EMSDK_PYTHON% %EMSCRIPTEN%\emcc.py %*
diff --git a/emscripten_toolchain/emcc.sh b/emscripten_toolchain/emcc.sh
index 5fdaf9c..f84dac0 100755
--- a/emscripten_toolchain/emcc.sh
+++ b/emscripten_toolchain/emcc.sh
@@ -2,4 +2,11 @@
 
 source $(dirname $0)/env.sh
 
-exec python3 $EMSCRIPTEN/emcc.py "$@"
+if [[ "$EMSDK_PYTHON" != /* ]]; then
+    EMSDK_PYTHON="$ROOT_DIR/$EMSDK_PYTHON"
+fi
+
+PYBINPATH="$(dirname "${EMSDK_PYTHON}")"
+export PATH=$PYBINPATH:$PATH
+
+exec $EMSDK_PYTHON $EMSCRIPTEN/emcc.py "$@"
diff --git a/emscripten_toolchain/emcc_link.sh b/emscripten_toolchain/emcc_link.sh
index 44f3235..96e5818 100755
--- a/emscripten_toolchain/emcc_link.sh
+++ b/emscripten_toolchain/emcc_link.sh
@@ -2,4 +2,11 @@
 
 source $(dirname $0)/env.sh
 
-exec python3 $(dirname $0)/link_wrapper.py "$@"
+if [[ "$EMSDK_PYTHON" != /* ]]; then
+    EMSDK_PYTHON="$ROOT_DIR/$EMSDK_PYTHON"
+fi
+
+PYBINPATH="$(dirname "${EMSDK_PYTHON}")"
+export PATH=$PYBINPATH:$PATH
+
+exec $EMSDK_PYTHON $EM_BIN_PATH/../../emscripten/emscripten/link.py "$@"
diff --git a/emscripten_toolchain/toolchain.bzl b/emscripten_toolchain/toolchain.bzl
index 80f55f6..f76f13b 100644
--- a/emscripten_toolchain/toolchain.bzl
+++ b/emscripten_toolchain/toolchain.bzl
@@ -1148,6 +1148,10 @@ def _impl(ctx):
 emscripten_cc_toolchain_config_rule = rule(
     implementation = _impl,
     attrs = {
+        "_py_interpreter": attr.label(
+            cfg = "exec",
+            default = Label("@rules_python//python:current_py_toolchain"),
+        ),
         "cpu": attr.string(mandatory = True, values = ["asmjs", "wasm"]),
         "em_config": attr.label(mandatory = True, allow_single_file = True),
         "emscripten_binaries": attr.label(mandatory = True, cfg = "exec"),
diff --git a/emscripten_toolchain/wasm_cc_binary.bzl b/emscripten_toolchain/wasm_cc_binary.bzl
index e7bd8ef..68fe28b 100644
--- a/emscripten_toolchain/wasm_cc_binary.bzl
+++ b/emscripten_toolchain/wasm_cc_binary.bzl
@@ -95,6 +95,10 @@ _WASM_BINARY_COMMON_ATTRS = {
         default = "@bazel_tools//tools/allowlists/function_transition_allowlist",
     ),
     "_wasm_binary_extractor": attr.label(
+    "_python_runtime": attr.label(
+        cfg = "exec",
+        default = Label("@rules_python//python:current_py_toolchain"),
+    ),
         executable = True,
         allow_files = True,
         cfg = "exec",
@@ -137,6 +141,18 @@ def _wasm_cc_binary_impl(ctx):
 
 def _wasm_cc_binary_legacy_impl(ctx):
     cc_target = ctx.attr.cc_target[0]
+    # Get Python runtime files for hermetic execution
+    py_files = ctx.attr._python_runtime[DefaultInfo].files.to_list()
+    python_bin_path = None
+    for f in py_files:
+        if f.basename == "python3" or "bin/python3" in f.path:
+            python_bin_path = f.path
+            break
+    
+    env = dict(ctx.configuration.default_shell_env)
+    if python_bin_path:
+        env["PATH"] = "%s:/usr/bin:/bin" % python_bin_path.rpartition("/")[0]
+
     outputs = [
         ctx.outputs.loader,
         ctx.outputs.wasm,
@@ -144,6 +160,7 @@ def _wasm_cc_binary_legacy_impl(ctx):
         ctx.outputs.data,
         ctx.outputs.symbols,
         ctx.outputs.dwarf,
+        ctx.outputs.dwp,
         ctx.outputs.html,
     ]
 
@@ -153,10 +170,11 @@ def _wasm_cc_binary_legacy_impl(ctx):
     args.add_joined("--outputs", outputs, join_with = ",")
 
     ctx.actions.run(
-        inputs = ctx.files.cc_target,
+        inputs = ctx.files.cc_target + py_files,
         outputs = outputs,
         arguments = [args],
         executable = ctx.executable._wasm_binary_extractor,
+        env = env,
     )
 
     return [
@@ -191,6 +209,7 @@ def _wasm_binary_legacy_outputs(name, cc_target):
         "data": "{}/{}.data".format(name, basename),
         "symbols": "{}/{}.js.symbols".format(name, basename),
         "dwarf": "{}/{}.wasm.debug.wasm".format(name, basename),
+        "dwp": "{}/{}.wasm.dwp".format(name, basename),
         "html": "{}/{}.html".format(name, basename),
     }
 
@@ -200,6 +219,7 @@ _wasm_cc_binary_legacy = rule(
     implementation = _wasm_cc_binary_legacy_impl,
     attrs = _WASM_BINARY_COMMON_ATTRS,
     outputs = _wasm_binary_legacy_outputs,
+    toolchains = ["@rules_python//python:toolchain_type"],
 )
 
 # Wraps a C++ Blaze target, extracting the appropriate files.
